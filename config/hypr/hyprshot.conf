[general]
save_dir = ~/Pictures/Screenshots
filename_pattern = %Y-%m-%d_%H-%M-%S
default_mode = region
copy_to_clipboard = false  # Better to control via keybinds
editor = swappy  # Optional for annotations
